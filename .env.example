# ========== 本地开发 ==========
# 数据库（本地可用 SQLite，或指向同一 Postgres）
DATABASE_URL="file:./dev.db"

# ========== Vercel 生产（必须在 Vercel 项目里配置） ==========
# Vercel Postgres 会在项目里自动注入，无需在 .env 写
# PRISMA_DATABASE_URL="prisma://..."   # 由 Vercel Storage 提供，在 Vercel Dashboard → Storage → Connection 里复制
# JWT_SECRET="<至少 32 字符的随机串>"   # 必须在 Vercel → Settings → Environment Variables 里添加
# 生成 JWT：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-secret-key-change-in-production"

# 管理后台端口（开发环境）
PORT=3000
ADMIN_PORT=true

# 管理员用户名（用于 set-admin 脚本）
ADMIN_USERNAME=""
