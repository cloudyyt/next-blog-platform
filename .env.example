# 数据库连接
# 开发环境：使用本地 SQLite
DATABASE_URL="file:./dev.db"

# 生产环境（Vercel）：使用 MySQL 或 PostgreSQL
# 如果使用 Vercel Postgres，部署后 Vercel 会自动配置这个环境变量
# DATABASE_URL="postgresql://user:password@host:5432/database?schema=public"

# 如果使用外部 MySQL（需要自行配置）
# DATABASE_URL="mysql://user:password@host:3306/database?schema=public"

# JWT 密钥（用于生成和验证 Token）
# ⚠️ 生产环境请使用强随机字符串，建议至少 32 个字符
# 可以使用以下命令生成：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-secret-key-change-in-production"

# 管理后台端口（开发环境）
PORT=3000
ADMIN_PORT=true

# 管理员用户名（用于 set-admin 脚本）
ADMIN_USERNAME=""
